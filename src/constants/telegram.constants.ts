export const PARSE_MODE = "HTML";

export const CONFIG_ACTION = {
  ACTIVE: "active",
  DEFAULT: "default",
} as const;

export const DELETE_CONFIG_PREFIX = "delete_config";

export const REGISTRATION_PREFIX = "reg";

export const CONFIG_SYNC_LIMIT = 20;

export const MAX_GROUP_TITLE_LENGTH = 60;
